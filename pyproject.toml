[tool.pytest.ini_options]
testpaths = ["proxy", "tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "S", "B"]
ignore = ["S101"]

[tool.ruff.lint.per-file-ignores]
"scripts/export_workflows.py" = ["S310"]

