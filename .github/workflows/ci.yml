name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install validation tools
        run: pip install pytest ruff yamllint
      - name: Validate
        run: ./scripts/validate.sh
      - name: Scan repository secrets
        uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Scan files and configuration
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: CRITICAL,HIGH
          exit-code: "1"

  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - context: docker/n8n
            dockerfile: docker/n8n/Dockerfile
            tag: n8n-snowflake:test
          - context: proxy/cortex
            dockerfile: proxy/cortex/Dockerfile
            tag: cortex-proxy:test
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4
      - name: Build linux/amd64 image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: ${{ matrix.image.context }}
          file: ${{ matrix.image.dockerfile }}
          platforms: linux/amd64
          pull: true
          push: false
          load: true
          tags: ${{ matrix.image.tag }}
          build-args: |
            N8N_VERSION=2.38.6
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Smoke test n8n image without credentials
        if: matrix.image.tag == 'n8n-snowflake:test'
        run: |
          docker run --rm --network none --entrypoint n8n n8n-snowflake:test --version
          docker run --rm --network none --entrypoint python3 n8n-snowflake:test -c 'import ssl, sqlite3; print(ssl.OPENSSL_VERSION)'
      - name: Smoke test proxy image without credentials
        if: matrix.image.tag == 'cortex-proxy:test'
        run: |
          docker run --rm --network none --entrypoint python3 cortex-proxy:test -c 'import runpy, ssl; module = runpy.run_path("/opt/cortex_proxy.py", run_name="smoke"); print(module["load_model_config"]())'
      - name: Scan built image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          scan-type: image
          image-ref: ${{ matrix.image.tag }}
          scanners: vuln
          format: json
          severity: CRITICAL,HIGH
          exit-code: "1"
